import 'package:flutter/material.dart';

class HomeCrownPage extends StatelessWidget {
  const HomeCrownPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Home_crown_page'),
      ),
    );
  }
}
